#include <bits/stdc++.h>

using namespace std;

int N;
vector<int> ninos;

int main () {
    while (true) {
        int N;
        cin >> N;
        ninos.clear();
        ninos.shrink_to_fit();
        int estado
        while (N-- != 0) {
            int tmp;
            cin >> tmp;
            ninos.push_back(tmp);
        }
    }
}